# Shared Packages

Place reusable, independently versioned workspace packages here. Application-specific code belongs in `apps/`.

The directory is intentionally empty today; add a package only when code is shared by at least two applications.
